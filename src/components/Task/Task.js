import React from 'react'

function Task({task, tasks, setTasks, index, dark}) {
  const handleComplete = e =>{
    e.preventDefault()
    
  }
  return (
    <div>Task</div>
  )
}

export default Task